import DesignKit

// The hand-off types travel with the components that draw them, so the host
// still sees one name for each without importing the design package itself.
public typealias TanyaAIAction = DesignKit.TanyaAIAction
public typealias TanyaAIActionButton = DesignKit.TanyaAIActionButton
public typealias TanyaAIActionPayload = DesignKit.TanyaAIActionPayload
